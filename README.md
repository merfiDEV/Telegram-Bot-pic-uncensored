# Telegram-Bot-pic-uncensored

---

# 🖼️ Bing Image Search Telegram Bot

Этот бот позволяет искать изображения через **Bing** напрямую из любого чата Telegram в **inline-режиме**. Просто вводите ник бота, запрос — и выбирайте картинку.

---

## ✨ Особенности

* **Inline Mode:** Работает в любом диалоге, не нужно добавлять бота в группы.
* **Умная фильтрация:** Реализована дедупликация изображений по MD5-хешам ссылок, чтобы выдача была чистой.
* **Бесконечная пагинация:** Поддерживает параметр `offset`, позволяя подгружать новые результаты при скролле.
* **Safe Search OFF:** В коде настроен обход фильтров контента (используйте ответственно!).

---

## 🚀 Быстрый старт

### 1. Подготовка окружения

Убедитесь, что у вас установлен Python 3.x. Установите необходимые библиотеки:

```bash
pip install pyTelegramBotAPI requests

```

### 2. Настройка токена

Откройте файл `picuncensored.py` и замените значение переменной `TOKEN` на токен вашего бота от [@BotFather](https://t.me/BotFather).

> **Важно:** В текущей версии токен уже вписан в код, но для безопасности лучше выносить его в переменные окружения (`.env`).

### 3. Запуск

```bash
python picuncensored.py

```

---

## 🛠 Технический стек

* **Библиотека:** `pyTelegramBotAPI` (Telebot) — для взаимодействия с Telegram API.
* **Парсинг:** `requests` + `re` (регулярные выражения) для извлечения прямых ссылок на изображения из Bing.
* **Хеширование:** `hashlib` для исключения повторов в выдаче.

---

## 📖 Как пользоваться

1. Напишите боту `/start`, чтобы прочитать дисклеймер.
2. Перейдите в любой чат.
3. В поле ввода сообщения напишите: `@имя_вашего_бота котики`
4. Выберите понравившуюся картинку из появившегося списка.

---

## ⚠️ Дисклеймер

Этот инструмент создан в ознакомительных целях. Бот не хранит изображения на своем сервере, а лишь транслирует результаты из открытых поисковых систем. Автор не несет ответственности за характер поисковых запросов пользователей.

---

**Разработано с ❤️ для удобного поиска контента.** Если вам помог этот проект, не забудьте поставить ⭐!

---
